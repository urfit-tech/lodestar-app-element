import { defineMessages } from 'react-intl'

const buttonsMessages = {
  Button: defineMessages({
    loginAlert: {
      id: 'button.loginAlert',
      defaultMessage: '請先登入',
    },
  }),
}

export default buttonsMessages
