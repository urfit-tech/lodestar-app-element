import { defineMessages } from 'react-intl'

const selectorsMessages = {
  CreditCardSelector: defineMessages({
    lastFourDigits: { id: 'selectors.creditCardSelector.lastFourDigits', defaultMessage: '末四碼：' },
    addCreditCard: { id: 'selectors.creditCardSelector.addCreditCard', defaultMessage: '新增信用卡' },
  }),
}

export default selectorsMessages
