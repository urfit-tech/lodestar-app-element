tasks:
  typecheck:
    command: 'tsc --noEmit'
    inputs:
      - 'src/**/*'
    deps:
      - 'types:typecheck'
      - 'helpers:typecheck'
      - 'graphql:typecheck'
      - 'contexts:typecheck'
      - 'hooks:typecheck'
